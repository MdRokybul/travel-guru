import React from 'react';

const NotMatch = () => {
    return (
        <div>
            <h3> 404 Error!!</h3>
            <h4> Page not found! </h4>
        </div>
    );
};

export default NotMatch;